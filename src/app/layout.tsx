import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Warriors Clan Quiz - Discover Your Inner Warrior",
  description: "Take our interactive quiz to discover which Warriors clan you belong to! Based on Erin Hunter's beloved book series, featuring ThunderClan, RiverClan, WindClan, ShadowClan, SkyClan, and Tribe of Rushing Water.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
